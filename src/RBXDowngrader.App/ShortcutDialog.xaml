<Window x:Class="RBXDowngrader.ShortcutDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Version shortcuts" Width="430" Height="330" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" WindowStyle="None" AllowsTransparency="True"
        Background="Transparent" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    <Border Margin="18" CornerRadius="20" Background="{StaticResource SurfaceBrush}"
            BorderBrush="#303030" BorderThickness="1">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="28" ShadowDepth="7" Opacity="0.65" />
        </Border.Effect>
        <Grid Margin="24,18,24,22">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="Version shortcuts" FontSize="18" FontWeight="SemiBold" />
            <TextBlock x:Name="VersionName" Grid.Row="1" Foreground="{StaticResource MutedBrush}"
                       FontSize="11" Margin="0,5,0,16" TextTrimming="CharacterEllipsis" />
            <StackPanel Grid.Row="2">
                <CheckBox x:Name="DesktopShortcut" Content="Desktop" FontSize="13" Margin="0,0,0,12" />
                <CheckBox x:Name="StartMenuShortcut" Content="Start Menu" FontSize="13" />
            </StackPanel>
            <Grid Grid.Row="4" Margin="0,22,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="1" Content="Cancel" Style="{StaticResource SecondaryButton}"
                        Width="88" Click="Cancel_Click" />
                <Button Grid.Column="2" Content="Save" Style="{StaticResource PrimaryButton}"
                        Width="88" Height="40" Margin="8,0,0,0" Click="Save_Click" />
            </Grid>
        </Grid>
    </Border>
</Window>
